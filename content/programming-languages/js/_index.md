+++
title = "js"
+++

# Javascript/Typescript

## Classes

The `#` operator can be used to declare private class fields and methods. The
`private` keyword can also be used to declare private fields but this is only a
compile-time feature and privacy is not enforced at runtime. The `#` operator
enforces privacy at runtime as well.
