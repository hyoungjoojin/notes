# Devices

## Device Controller
- A device controller is a hardware unit that can be used to handle the hardware devices by acting as an interface between the hardware device and device driver.
- It can have its own local buffer storage and registers.

## Device Driver
- A device driver is a software module that acts as an interface between the kernel and device controller.
- Each device controller has its own device driver.

